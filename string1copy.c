#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}

void print_msg(char* str[3])
{
 int i=0;
 for(i=0;i<3;i++)
 printf("The message passed is %s\n",str[i]);
}

extern char **environ;
 
int main()
{
 char* s[3] = {"Hello","Kashi","How are you?"};
 print_msg(s);
 printf("The environment variables are as follows:\n");
 char* envir = *environ;
 char* temp =NULL;
 for( ;*environ != NULL; environ++)
 {
	temp = *environ;
	printf("%s\n",temp);
 }
 return 0;
}
